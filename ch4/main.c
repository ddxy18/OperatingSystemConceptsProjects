#include "p2.c"

int main() {
    /*setSudoku();
    validate();
    for (int i = 0; i < SIZE; ++i) {
        free(*(sudoku + i));
    }*/

    /*setArray();
    sort();
    for (int i = 0; i < a->size; ++i) {
        printf("%d",  a->array[i]);
    }
    free(a->array);
    free(a);*/

    return 0;
}
